# 调试入口

本目录中的 `cheat-gui.py` 仅用于排查网页修改器无法连接的问题。它会在游戏启动时显示
HTTP 服务结果、WebSocket 实际端口，以及 `pgvz`、`pgvztool`、`gui` 关键文件的检查结果，
帮助区分入口未加载、端口异常、网络拦截和安装文件缺失。

使用时，用本目录中的脚本替换游戏 `mods/cheat-gui.py` 并重启游戏。看到诊断弹窗后先将其
关闭，再打开网页测试；弹窗关闭前 WebSocket 尚未开始监听。排查完成后请恢复发布包根目录
中的正式 `cheat-gui.py`，不要使用调试入口进行正式游玩。
